@model IEnumerable<Rent_A_Car.Models.AutoFoto1>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Foto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Foto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Bestandsnaam">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Bestandsnaam">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Bestandsnaam">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
